# @package _global_
defaults:
  - /hardware: cpu_smoketest
  - _self_

model_name: "microsoft/Florence-2-base"
data:
  metadata_path: "output/synthetic_v1/metadata.jsonl"
  max_train_samples: 32
  val_fraction: 0.1
lora:
  r: 8
  alpha: 16
  dropout: 0.05
  target_modules: ["q_proj", "k_proj", "v_proj", "out_proj"]
learning_rate: 1e-4
logging_steps: 1
output_dir: "output/train/vlm_sft/run"
save_adapter_dir: "output/train/vlm_sft/adapter"
