playwright>=1.45
jinja2>=3.1
faker>=25.0
datasets>=2.19
huggingface_hub>=0.23
hydra-core>=1.3
omegaconf>=2.3
pillow>=10.0
# torch/torchvision must be installed as a matched pair from the CPU wheel index, e.g.:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cpu
# Installing torchvision from plain PyPI against a mismatched torch build breaks
# torchvision's compiled ops (fails with "operator torchvision::nms does not exist").
torch==2.6.0
torchvision==0.21.0
# Pinned <5.0: Florence-2's trust_remote_code config class breaks on transformers 5.x
# (PretrainedConfig attribute-access changes). 4.49.0 confirmed working with both
# Qwen2.5-Instruct and Florence-2-base.
transformers==4.49.0
accelerate>=0.30
timm>=1.0
einops>=0.7
peft>=0.11
